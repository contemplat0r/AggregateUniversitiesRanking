# coding: utf-8

import pandas_and_name_matching_exp_2_clean
import pandas_and_name_matching_exp_2_clean
s = 'a'
s
s.upper()
s
import pandas_and_name_matching_exp_2_clean
import pandas_and_name_matching_exp_2_clean
len(s)
import numpy as np
import pandas_and_name_matching_exp_2_clean
import pandas_and_name_matching_exp_2_clean
from pandas_and_name_matching_exp_2_clean import *
reload(pandas_and_name_matching_exp_2_clean)
from pandas_and_name_matching_exp_2_clean import *
qs_university_name_variants_list[0]
reload(pandas_and_name_matching_exp_2_clean)
from pandas_and_name_matching_exp_2_clean import *
qs_name_variants_0 = get_name_variants(qs_university_names_list[0])
import name_matching
reload(name_matching)
reload(name_matching)
from name_matching import *
qs_name_variants_0 = get_name_variants(qs_university_names_list[0])
reload(name_matching)
from name_matching import *
qs_name_variants_0 = get_name_variants(qs_university_names_list[0])
qs_name_variants_0
s
s1 = '(abc)'
s1.strip('()')
s1
reload(name_matching)
from name_matching import *
qs_name_variants_0 = get_name_variants(qs_university_names_list[0])
reload(name_matching)
from name_matching import *
qs_name_variants_0 = get_name_variants(qs_university_names_list[0])
qs_name_variants_0
reload(name_matching)
from name_matching import *
qs_name_variants_0 = get_name_variants(qs_university_names_list[0])
qs_name_variants_0
the_university_names = the_df['university_name']
the_university_names
the_university_names[the_university_names['unversity_name'].map(lambda x: x.find('-') != -1)]
the_university_names[the_df['unversity_name'].map(lambda x: x.find('-') != -1)]
the_university_names[the_df['unversity_name'].map(lambda x: x.find('-')) != -1]
the_df['university_name']
the_university_names[the_df['university_name'].map(lambda x: x.find('-')) != -1]
the_university_names_with_dash_list = list(the_university_names[the_df['university_name'].map(lambda x: x.find('-')) != -1])
len(the_university_names_with_dash_list)
the_university_names_with_dash_list
leiden_university_names_with_dash_list = list(leiden_university_names[the_df['university_name'].map(lambda x: x.find('-')) != -1])
leiden_university_names = leiden_df['university_name']
leiden_university_names_with_dash_list = list(leiden_university_names[the_df['university_name'].map(lambda x: x.find('-')) != -1])
leiden_university_names_with_dash_list = list(leiden_university_names[leiden_df['university_name'].map(lambda x: x.find('-')) != -1])
len(leiden_university_names_with_dash_list)
leiden_university_names_with_dash_list
qs_university_names = qs_df['university_name']
the_university_names_with_dash_list = list(qs_university_names[qs_df['university_name'].map(lambda x: x.find('-')) != -1])
qs_university_names_with_dash_list = list(qs_university_names[qs_df['university_name'].map(lambda x: x.find('-')) != -1])
len(qs_university_names_with_dash_list)
qs_university_names_with_dash_list
the_university_names_with_dash_list = list(the_university_names[the_df['university_name'].map(lambda x: x.find('-')) != -1])
